{
  "name": "poap-w3q-subgraph",
  "license": "UNLICENSED",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "postinstall": "mustache config/w3q.json subgraph.template.yaml > subgraph.yaml && npm run codegen && npm run build"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0",
    "mustache": "^4.2.0"
  }
}
